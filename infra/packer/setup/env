# This is a demonstration environment variable, the following commented lines are required to build filedrop server
# You can uncomment and change the value as desired for the build

#SSH_PASSWORD=glasswall
#BRANCH=packer-templates
#GW_SDK_PORT=1346
